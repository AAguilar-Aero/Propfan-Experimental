# Verification tests

Add a test with every reusable engineering module. Prefer analytical solutions, dimensional checks, published benchmark cases and regression tests over tests that merely reproduce an implementation detail.
