# Model configuration

Store versioned, human-readable inputs here. Use YAML or TOML for mission, atmosphere, component and optimisation cases once the requirements baseline is frozen.

Configuration values must use SI units and refer to an entry in `docs/ASSUMPTIONS.md` or an external source. Do not place credentials, proprietary data or local absolute paths in this directory.
